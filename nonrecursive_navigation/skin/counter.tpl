<!-- This is the page counter HTML. Filthy HTML code but its only a simple example. It would be possible to include a custom CSS file. -->
<div style="background-color: {$MODULE_CONFIG.background_color}; border: 3px solid {$MODULE_CONFIG.border_color}; color: {$MODULE_CONFIG.text_color}; padding: 10px; width: 200px; font-family: {$MODULE_CONFIG.font_family}">
{$MODULE_LANG.page_views} {$PAGE_VIEWS}
</div>